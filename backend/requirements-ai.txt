# Optional extra for the Gemini 3.6 Flash LLM backend (design doc §6.1).
#
# The default LLM backend is a deterministic narrator (no API key, no cost,
# reproducible), so this is NOT in the base image. Install it and set
# GEMINI_API_KEY to generate report/recommendation prose with Gemini:
#
#   pip install -r requirements-ai.txt
#
# Note: google-genai requires pydantic >= 2.13 (the base pin allows this).

google-genai>=2.16,<3
